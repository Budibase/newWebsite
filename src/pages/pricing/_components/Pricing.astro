---
/* removed self-host switcher imports */
import PricingCard from "./PricingCard.astro";
import AddOnsCard from "./AddOnsCard.astro";
import Link from "@components/Link.astro";
---

<section class="container max-w-7xl mx-auto flex flex-col mt-24">
  <!--Title section -->
  <h1 class="max-w-2xl
    mb-4  text-stone-950 dark:text-stone-100">
    Pricing
  </h1>
  <div class="mb-6 text-secondary">
    We also have an <span>
      <Link
        url="https://github.com/Budibase/budibase"
        label="open-source community plan"
        variant="primary"
      />
    </span>you can self-host.
  </div>
  <div class="container flex flex-col gap-2">
    <div class="flex flex-row gap-2 mb-4">
      <ul class="inline-flex flex-row bg-secondary rounded-[20px] p-1">
        <li>
          <button
            id="Annual button"
            data-billing="annual"
            aria-pressed="true"
            class="bg-white dark:bg-stone-800 rounded-[19px] py-2 px-2 flex flex-row text-stone-900 dark:text-stone-100"
          >
            Yearly
          </button>
        </li>
        <li>
          <button
            id="Monthly button"
            data-billing="monthly"
            aria-pressed="false"
            class="bg-transparent rounded-[19px] py-2 px-2 flex flex-row text-stone-600 dark:text-stone-400"
          >
            Monthly
          </button>
        </li>
      </ul>
    </div>

    <section class="mb-20">
      <div class="grid grid-cols-3 gap-3">
        <PricingCard
          title="Pro"
          description="Includes:"
          creatorPrice={10}
          userPrice={2}
          creatorUnit="/ month"
          userUnit="/ month"
          creatorLabel="per creator"
          userLabel="per end user"
          recommended={false}
          buttonLabel="Start 14 day free trial"
          buttonVariant="primary"
          buttonUrl="/signup-plus"
          features={[
            "1 creator",
            "1 Workspace",
            "10k Automation runs",
            "Custom RBAC",
            "Custom branding",
            "Embeds",
          ]}
        />
        <PricingCard
          title="Premium"
          description="Everything in Pro, plus:"
          creatorPrice={50}
          userPrice={5}
          creatorUnit="/ month"
          userUnit="/ month"
          creatorLabel="per creator"
          userLabel="per end user"
          recommended={true}
          buttonLabel="Start 14 day free trial"
          buttonVariant="cta"
          buttonUrl="/signup-plus"
          features={[
            "No creator limit",
            "Multiple workspaces",
            "100k Automation runs",
            "User groups",
            "Audit Logs",
            "Synchronous automations",
          ]}
        />
        <PricingCard
          title="Enterprise"
          description="Everything in Premium, plus:"
          creatorPrice={200}
          userPrice={10}
          creatorUnit="/ month"
          userUnit="/ month"
          creatorLabel="per creator"
          userLabel="per end user"
          recommended={false}
          buttonLabel="Contact sales"
          buttonVariant="primary"
          buttonUrl="/signup-plus"
          features={[
            "Bulk discounts",
            "SSO",
            "On-premise / Cloud",
            "Active directory / SCIM",
            "Env variables",
            "Dedicated Support",
          ]}
        />
      </div>
    </section>
    <section class="mt-8">
      <p class="text-2xl! pb-6 text-stone-900 dark:text-stone-100">Add-ons</p>
      <AddOnsCard label="Airgapped deployment" price="$10,000 per annum" />
      <AddOnsCard label="1 business day SLA" price="$20,000 per annum" />
      <AddOnsCard label="Professional services" price="$500 per day" />
    </section>
  </div>
</section>
