<section class="container mx-auto mt-12 md:mt-20 px-2 lg:px-0">
  <div class="letter-card relative mx-auto max-w-5xl rounded-2xl border border-stone-200 bg-stone-50 px-6 py-8 sm:px-10 sm:py-12  ">
    <div class="pointer-events-none absolute inset-0 rounded-2xl border border-stone-200/70 "></div>
    <div class="pointer-events-none absolute inset-4 rounded-xl border border-stone-200/50 "></div>
    <div class="pointer-events-none absolute right-6 top-6 h-20 w-20 rounded-full border border-stone-300/60 text-stone-400/70  ">
      <div class="absolute inset-2 rounded-full border border-current"></div>
      <div class="absolute inset-6 rounded-full border border-current"></div>
    </div>

    <p class="text-xs uppercase tracking-[0.3em] text-stone-500 ">
      A letter
    </p>
    <h2 class="mt-2 text-3xl font-semibold text-stone-800 ">
      From the team
    </h2>

    <div class="mt-5 space-y-4 text-base leading-relaxed text-stone-700 ">
      <p>
        Open source is in our DNA, and we are building the AI era in public. We believe the best tools
        are transparent, extensible, and owned by the teams who rely on them.
      </p>
      <p>
        Budibase is a privacy-first, open platform for automating workflows with AI. It helps teams
        ship agents, chat, and internal apps without handing over their data.
      </p>
    </div>

    <div class="mt-8 text-sm text-stone-600 ">
      <p class="font-semibold text-stone-800 ">The Budibase team</p>
      <p>Product and engineering</p>
    </div>
  </div>
</section>

<style>
  .letter-card {
    background-image:
      repeating-linear-gradient(
        -45deg,
        color-mix(in srgb, var(--bb-gray-500) 8%, transparent),
        color-mix(in srgb, var(--bb-gray-500) 8%, transparent) 2px,
        transparent 2px,
        transparent 10px
      ),
      radial-gradient(color-mix(in srgb, var(--bb-gray-500) 8%, transparent) 1px, transparent 1px),
      radial-gradient(color-mix(in srgb, var(--bb-orange-600) 42%, transparent) 2.2px, transparent 2.2px),
      radial-gradient(color-mix(in srgb, var(--bb-red-600) 38%, transparent) 2px, transparent 2px),
      radial-gradient(color-mix(in srgb, var(--bb-orange-400) 40%, transparent) 2px, transparent 2px);
    background-size: 100% 100%, 22px 22px, 20px 20px, 18px 18px, 26px 26px;
    background-position: 0 0, 8px 8px, 6px 2px, 2px 14px, 14px 18px;
    background-blend-mode: normal;
  }
</style>
